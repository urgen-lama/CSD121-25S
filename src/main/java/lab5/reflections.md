2.5 We can change the type of the returned value in promptForPlayer without changing the return type in the function signature because of polymorphism. Polymorphism allows objects to be treated as instances of their superclass. HumanPlayer class inherits from the Player class and it establishes "is-a" relationship. Because of this when a player is called, any player i.e. HumanPlayer, Linus, or Omola can be called and does not affect the return type. 

2.9 The error occured initially because after moving the pickNextMove method from Player class to HumanPlayer class, the player class no longer contained a method named pickNextMove. By adding the abstract method signature, it works because it tells compiler that every player type will have a pickNextMove method. Even if the other player classes choose other way of picking move.

5 It is possible that neither our main method nor our TicTacToeGame class needs change at all when adding new Player types to our game because the Player class acts as an abstraction class, where all the other players are related. By declaring pickNextMove(board) as an abstract method, Player mandates that any class extending it must provide its own specific implementation for determining a move, without TicTacToeGame or Main needing to know how that move is decided. The whoseTurn.pickNextMove(board) call works correctly for any player type because of polymorphism. The Player class sets a contract that all player types will have a pickNextMove method.